---
"@agentionai/marshall-cli": minor
"@agentionai/marshall-engine": minor
---

Add typeahead search to the model picker, retain OpenRouter text models without tool support with a warning indicator, and include content-safety models in the catalogue. Replace the transient busy spinner with an activity status row showing request state, queued prompts, token usage, duration, and approximate output throughput.
