---
'@agentionai/marshall-cli': patch
---

Reasoning is now shown by default, so a reasoning model's thinking is visible
from the first run instead of needing Ctrl-R to discover it exists. Ctrl-R
still toggles it off.
