{
  "name": "agention-marshall",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "cli": "node --import tsx/esm apps/cli/src/index.tsx",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0"
  },
  "dependencies": {
    "@agentionai/agents": "^1.1.0",
    "openai": "^6.42.0"
  }
}
