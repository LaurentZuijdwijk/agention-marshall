{
  "name": "agention-marshall",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "cli": "npm run build -w packages/tools -w packages/engine && node --import tsx/esm apps/cli/src/index.tsx",
    "build": "npm run build -w packages/tools -w packages/engine",
    "build:all": "npm run build -w packages/tools -w packages/engine -w apps/cli",
    "pretest": "npm run build",
    "test": "npm run test --workspaces --if-present",
    "release": "npm test && npm run build:all && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0"
  },
  "dependencies": {
    "@agentionai/agents": "1.6.0-beta.0",
    "openai": "^6.42.0"
  }
}
