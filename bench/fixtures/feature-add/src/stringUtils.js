/**
 * Spec: implement titleCase(str) — capitalize the first letter of every
 * word in `str` and lower-case the rest of each word. Words are separated
 * by single spaces. Example: titleCase('hello world') === 'Hello World'.
 */
function titleCase(str) {
  throw new Error('not implemented');
}

module.exports = { titleCase };
